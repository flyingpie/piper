<NodeView Node="Node">

	@foreach (var prop in Node.NodeProps)
	{
		if (prop is Core.PpNodeParam param)
		{
			<PpNodeParam Param="param" />
		}

		else if (prop is Core.PpNodePort port)
		{
			<PpNodePort NodePort="port"/>
		}
	}

</NodeView>